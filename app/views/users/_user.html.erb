<li>
  <%= link_to user.depto + " " + user.condo, user %>

  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "edit",  edit_user_path(user.id) %>
    | <%= link_to "delete", user, method: :delete, data: { confirm: "¿Seguro, carnal?" } %>

    <% end %>
</li>
